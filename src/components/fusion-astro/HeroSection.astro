---
import { ArrowRight } from "lucide-react";

interface Props {
  // Add props if we want to make content dynamic later
}
---

<section class="min-h-[90vh] flex items-center pt-32 pb-20 px-6 relative overflow-hidden">
    <!-- Background Enhancement -->
    <div class="absolute inset-0 -z-10">
        <div class="absolute inset-0 bg-grid-mesh [mask-image:radial-gradient(ellipse_60%_50%_at_50%_0%,#000_70%,transparent_100%)] opacity-30"></div>
        <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[1000px] h-full bg-blue-500/5 blur-[120px] rounded-full pointer-events-none"></div>
    </div>

    <div class="max-w-7xl mx-auto w-full">
        <div class="max-w-4xl">
            <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-blue-500/10 border border-blue-500/20 text-blue-400 text-[10px] font-black uppercase tracking-[0.2em] mb-8 animate-fade-in">
                <span class="relative flex h-2 w-2">
                    <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                    <span class="relative inline-flex rounded-full h-2 w-2 bg-blue-500"></span>
                </span>
                Available for new opportunities
            </div>

            <h1 class="text-fluid-h1 font-black mb-8 animate-slide-up">
                Engineering at the <br /> 
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 via-blue-200 to-purple-400 animate-gradient-text">Intersection</span> of Intelligence.
            </h1>

            <p class="text-xl md:text-2xl text-gray-400 mb-12 max-w-2xl leading-relaxed animate-slide-up opacity-90" style="animation-delay: 0.1s">
                Architecting high-complexity systems across <strong>Quantitative Trading</strong>, <strong>Real-time Media Protocols</strong>, and <strong>Web3 Infrastructure</strong>.
            </p>

            <div class="flex flex-wrap gap-6 animate-slide-up" style="animation-delay: 0.2s">
                <a
                    href="#projects"
                    class="px-10 py-4 bg-white text-black font-bold rounded-full hover:bg-blue-50 transition-all hover:scale-105 hover:shadow-[0_0_30px_rgba(255,255,255,0.3)] flex items-center gap-2 active:scale-95"
                >
                    Explore Lab Projects <ArrowRight size={20} />
                </a>
                <a
                    href="/questions"
                    class="px-10 py-4 bg-blue-500/10 text-blue-400 font-bold rounded-full border border-blue-500/20 hover:bg-blue-500/20 hover:border-blue-500/40 transition-all hover:scale-105 flex items-center gap-2 active:scale-95"
                >
                    Technical Arsenal <ArrowRight size={20} />
                </a>
            </div>
        </div>
    </div>
</section>

<style>
    .animate-gradient-text {
        background-size: 200% auto;
        animation: shine 6s linear infinite;
    }
    @keyframes shine {
        to { background-position: 200% center; }
    }
    @keyframes fade-in {
        from { opacity: 0; }
        to { opacity: 1; }
    }
    @keyframes slide-up {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .animate-fade-in { animation: fade-in 1s ease-out forwards; }
    .animate-slide-up { animation: slide-up 1s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
</style>
